
---

## ⚡ Getting started (local)
**Prerequisites**
- .NET SDK 7.0 or later (`dotnet --version`)
- SQL Server or Azure SQL (connection string)
- Redis (optional for caching)
- Docker (optional)

**Local steps**
1. Clone:
   ```bash
   git clone <REPO_URL>
   cd <REPO_FOLDER>

(You can replace the port placeholder later with actual one shown by `dotnet run`.)

---

## 2) .gitignore (`.gitignore`)
Create a file `.gitignore` at repo root and paste the standard .NET ignore:

